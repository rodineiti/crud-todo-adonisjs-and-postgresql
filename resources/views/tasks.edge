@layout('main')
@section('content')
<h1>Tasks</h1>
@if(old('notification'))
<div class="alert alert-success" role="alert">
  {{old('notification')}}
</div>
@endif
<div class="list-group">
  @each(task in tasks)
    <a href="/show/{{task.id}}" class="list-group-item list-group-item-action">{{task.title}}</a>
  @endeach
</div>
@endsection
